import React from "react";

const Navbar: React.FC = () => {
  
  // pague os pages da rotas do router de dentro do main.tsx
    // const pages = ["Dashboard", "CreateAccount", "Transactions"];

  return (
    <nav className="nav">
      <ul>
        <label>Home</label>
       
      </ul>
    </nav>
  );
};

export default Navbar;
